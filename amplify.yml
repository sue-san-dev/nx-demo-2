version: 1
applications:
  - appRoot: apps/client
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - nx build client
      artifacts:
        baseDirectory: dist\apps\client
        files:
          - '**\*'